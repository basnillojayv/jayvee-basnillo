Impact statistic — big navy numeral counting up from zero, plain label underneath.

```jsx
<StatCounter value={48} suffix="+" label="Events Held" />
<StatCounter value={96} suffix="%" label="Fellowship Completion Rate" />
```

Only ever render real figures. Three across in a row on cream.
