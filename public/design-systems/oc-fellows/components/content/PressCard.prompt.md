Press clipping card — portrait scan above the publication name. Links out, so it pairs with an `external-link` glyph.

```jsx
<PressCard publication="OCBJ Leaderboard" date="July 2025" thumbnail="…" href="…" />
```

Four across on white. Uses a hairline border rather than a shadow, so scans read as documents.
