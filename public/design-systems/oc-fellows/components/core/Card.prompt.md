Base card surface — 10px radius, white on cream. Pick `edge="shadow"` or `edge="border"`, never both.

```jsx
<Card interactive href="/stories/kendra">
  <h4>Life Sciences Engineer Builds Community as an OC Fellow</h4>
</Card>
```

All the content cards (StoryCard, PersonCard, PressCard, ProgramCard) are built on this.
