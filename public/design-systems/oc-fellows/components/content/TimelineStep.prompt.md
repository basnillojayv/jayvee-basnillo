Numbered step for the six-stage application timeline. Stack them in a flex column; mark the final one `last`.

```jsx
<TimelineStep number={1} title="Application Period"
  description="Application opens on March 1st each year. The application will be open until the end of April" />
```

Numerals in navy circles; `active` turns one orange. Never substitute icons for the numbers.
